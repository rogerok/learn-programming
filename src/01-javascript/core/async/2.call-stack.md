---
tags: [javascript, async, call-stack, v8, runtime]
aliases: [Call Stack, Стек вызовов]
---
# Как устроена среда исполнения

На схеме ниже — упрощенное представление среды исполнения Runtime.
В частности, движок V8 представлен как среда выполнения команд внутри браузера Google Chrome:

![img.png](img.png)

Распределение памяти происходит в `heap`, а stack frames хранятся в стеке вызовов (call stack)

**Стек вызовов** - механизм, предназначенный для отслеживания текущего местонахождения интерпретатора в скрипте.
Он вызывает несколько ф-ций и определяет, какая из них выполняется на данный момент, какие ф-ции вызываются внутри выполняемой ф-ции.
Рассмотрим пример кода ниже: baz — функция, которая после запуска в Chrome вызывает bar, затем foo и возвращает ошибку.

![img_1.png](img_1.png)

Стек вызовов создается, когда внутри функции (или метода) существуют другие функции.
В качестве результата выполнения функция выводит на экран трассировку стека (stack trace) — состояние стека в тот момент, когда произошла ошибка.

![img_2.png](img_2.png)

# Асинхронные вызовы

Рассмотрим на простом примере, как работают асинхронные вызовы:

![img_3.png](img_3.png)

При запуске ф-ция `setTimeout` ставит `console.log` в очередь на пять секунд, в это время выводится `JSconfEU`, а в конце выводится `there`

# Цикл событий

Цикл событий следит за стеком и очередью задач.
Если стек пуст, цикл берет первый элемент из очереди, помещает его в стэк и выполняет.
![img_4.png](img_4.png)

На картинке выше видно, что если стек пуст, то цикл берет первый элемент из очереди задач,и помещает его в стек.
В очереди задач при этом находится ф-ция `cb`

Первая сложность - установка нулевого времени в ф-ции `setTimeout`, колбек выполняется не сразу, а попадает в очередь задач.
Причина в работе механизма цикла событий - он ждет очищения стека и только после этого переходит к выполнению очереди задач.

Установка нулевого времени в ф-ции `setTimeout` приводит к тому, что она откладывает исполнение колбека и переносит его в конец стека.

Все web-API работают похожим образом. Например, AJAX-запрос на URL-адрес с обратным вызовом будет выполняться точно также.

XHR запрос будет выполняться параллельно, он может быть не выполнен никогда, но стек при этом будет продолжать работать.

![img_5.png](img_5.png)

# Рендеринг

В идеальной ситуации, браузер перерисовывает экран раз в 16.6 миллисекунд, и воспроизводит результат в 60 fps.

НО, если в стеке есть код, браузер не может проводить рендеринг.

Если рендер назвать вызовом, то в нем почти есть всегда коллбэк, который должен дождаться очистки стека.
Разница в том, что приоритет рендера выше - каждые 16 мс, он ставит отрисовку в очередь.

---
tags: [javascript, async, error-handling, try-catch, callbacks]
aliases: [Обработка ошибок в async]
---
# Обработка ошибок

В JavaScript обработка ошибок работает через механизм исключений. Одни функции их возбуждают, другие обрабатывают через try..catch. Так было в синхронном коде. В асинхронном стандартный механизм уже не работает.

```ts
import fs from "fs";

try {
  // try to read directory - it's an error
  fs.readFile("./directory", "utf-8", () => {
    callUndefinedFunction();
  });
} catch (e) {
  console.log("error");
}
```

`try\catch` работает только с кодом из текущего стека вызовов и он не может перехватить то, что вызвалось в другом стеке.
Поэтому мы не увидим сообщения `"error!"`, хотя сама ошибка появится

```
callUndefinedFunction();
^

ReferenceError: callUndefinedFunction is not defined
    at ReadFileContext.fs.readFile [as callback] (/private/var/tmp/index.js:6:5)

```

Из вывода видно, что колбек вызвался в своем стеке вызовов, начавшимся внутри ф-ции `readFile`, фактически это означает, что использовать `try\catch` в асинхронном коде с коллбеками - бесполезно.

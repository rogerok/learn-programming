# never
Тип `never` используется, когда ф-ция гарантировано ничего не возвращает, например:
- рекурсия
- функция пробрасывает исключение (throw new Error())
- функция завершает программу

Важным условием использования типа `never` является отсутствие нормального завершения ф-ции.
